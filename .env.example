# Firebase (login com Google) - https://console.firebase.google.com
# Authentication > Sign-in method > Google
# App Web > copie o config
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# MongoDB Atlas (progresso do aluno) - https://cloud.mongodb.com
# Crie um cluster Free, Database Access, Network Access (0.0.0.0/0), copie a connection string
MONGODB_URI=mongodb+srv://usuario:senha@cluster.xxxxx.mongodb.net/desorientado?retryWrites=true&w=majority

# Firebase Admin (só no Vercel / backend) - para a API validar o token
# Firebase Console > Configurações do projeto > Contas de serviço > Gerar nova chave privada
# Cole o JSON inteiro em uma linha (ou use variável de ambiente única)
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
